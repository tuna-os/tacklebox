# Build artifacts
/tacklebox
/tbox-*
/loop_dev

# Build output
/build/
/dist/

# Test artifacts
/coverage.out
*.test

# Agent runtime artifacts
.pi/goals/

# Per-developer Claude Code permissions. settings.json is the shared file;
# settings.local.json is personal and should never have been committed.
.claude/settings.local.json
